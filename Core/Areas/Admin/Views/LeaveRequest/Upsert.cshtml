@model LeaveRequestVM

<div class="card shadow border-0 my-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2"> @(Model.LeaveRequest.Id != 0 ? "Create" : "Update") Leave Request</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row" enctype="multipart/form-data">
            <input asp-for="LeaveRequest.Id" hidden />
            <div class="row">
                <div class="col-12">
                    <div class="border p-3">
                        <div asp-validation-summary="ModelOnly"></div>

                        <div class="form-floating py-2 col-12">
                            <input asp-for="LeaveRequest.EmployeeEmail" class="form-control border-0 shadow" />
                            <label asp-for="LeaveRequest.EmployeeEmail" class="ms-2"></label>
                            <span asp-validation-for="LeaveRequest.EmployeeEmail" class="text-danger"></span>
                        </div>
                        <div class="form-floating py-2 col-12">
                            @if (Model.LeaveRequest.Id != 0)
                            {
                                <input asp-for="LeaveRequest.Start" class="form-control border-0 shadow" type="date" />
                            }
                            else
                            {
                                <input asp-for="LeaveRequest.Start" class="form-control border-0 shadow" type="date" value="@DateOnly.FromDateTime(DateTime.Now)" />
                            }
                            <label asp-for="LeaveRequest.Start" class="ms-2"></label>
                            <span asp-validation-for="LeaveRequest.Start" class="text-danger"></span>
                        </div>
                        <div class="form-floating py-2 col-12">
                            @if (Model.LeaveRequest.Id != 0)
                            {
                                <input asp-for="LeaveRequest.End" class="form-control border-0 shadow" type="date" />
                            }
                            else
                            {
                                <input asp-for="LeaveRequest.End" class="form-control border-0 shadow" type="date" value="@DateOnly.FromDateTime(DateTime.Now)" />
                            }
                            <label asp-for="LeaveRequest.End" class="ms-2"></label>
                            <span asp-validation-for="LeaveRequest.End" class="text-danger"></span>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <select asp-for="@Model.LeaveRequest.Type" asp-items="@Model.Types" class="form-select border-0 shadow">
                                <option disabled selected>Select Type of Leave Request</option>
                            </select>
                            <label asp-for="LeaveRequest.Type" class="ms-2"></label>
                            <span asp-validation-for="LeaveRequest.Type" class="text-danger"></span>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <textarea asp-for="LeaveRequest.Reason" class="form-control border-0 shadow" style="height: 150px;"></textarea>
                            <label asp-for="LeaveRequest.Reason" class="ms-2"></label>
                            <span asp-validation-for="LeaveRequest.Reason" class="text-danger"></span>
                        </div>
                        <div class="row pt-2">
                            <div class="col-6 col-md-3">
                                @if (Model.LeaveRequest.Id != 0)
                                {
                                    <button type="submit" class="btn btn-primary form-control">Update</button>
                                }
                                else
                                {
                                    <button type="submit" class="btn btn-primary form-control">Create</button>
                                }
                            </div>
                            <div class="col-6 col-md-3">
                                <a asp-area="Admin" asp-controller="LeaveRequest" asp-action="Index" class="btn btn-outline-primary border  form-control">
                                    Back to List
                                </a>
                            </div>
                        </div>


                    </div>
                </div>

            </div>

        </form>
    </div>
</div>




@section Scripts {
    @{
        <partial Start="_ValidationScriptsPartial" />
    }
}